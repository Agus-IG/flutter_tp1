// DECLARACIÓN DE VARIABLES

// Declara tres variables: un entero, un número decimal y una cadena de
// texto. Asigna valores iniciales y muestra estos valores en la consola.

void main(){
  int numEntero = 1; //Se utiliza int para declarar que el valor sera un numero entero
  double numDecimal = 1.5; //Se utiliza double para que declarar que el valor sera un numero decimal
  String texto = 'Hello world!'; // Se utiliza String para declarar que el valor sera una cadena de texto

  //Mostramos los valores por consola utilizando print
  print(numEntero);
  print(numDecimal);
  print(texto);
}